# Maven
/target/
/.idea/
.classpath
.project
.settings/
*.log
*.class
bin/
temp/
.DS_Store
/logs/
/build/ # Gradle build output
.gradle/ # Gradle wrapper files
!/.gradle/wrapper # Keep the wrapper files
# STS (Spring Tool Suite)
.apt_generated/
.factorypath
.metadata/

# For Spring Boot applications
*.jar
*.war
*.zip
!*.jar!/  # Allow jar files within jar files (e.g., for Spring Boot fat jars)
!**/src/main/**/resources/**/*.jar # Keep resource jars

# Environment Variables
.env
.env.*

# Local specific configurations
*.iml # IntelliJ IDEA project files
.vscode/ # VS Code project files (if not shared)

# Optional: Sensitive files (e.g., local database configs)
backend/src/main/resources/application.properties
# application-local.properties
# application-dev.properties
# application-prod.properties # Or specific profiles